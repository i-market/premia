<div class="admin-table">
    <div class="top">
        <h3 class="heading">{{ table.NAME }}</h3>
        <a href="{{ download_link }}" target="_blank" download class="button btn--blue">Скачать CSV файл</a>
    </div>
    <div class="table-responsive">
        <table class="preview">
            <thead>
            <tr>
                {% for col in table.HEADER %}
                    <th>{{ col }}</th>
                {% endfor %}
            </tr>
            </thead>
            <tbody>
            {% for row in table.ROWS %}
                <tr>
                    {% for col in row %}
                        <td>{{ col }}</td>
                    {% endfor %}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>